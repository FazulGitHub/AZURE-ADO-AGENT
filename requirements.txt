Flask>=3.0.0
openai>=1.30.0
python-dotenv>=1.0.1
gunicorn>=21.2.0

# Note: we use the official `openai` SDK pointed at Groq's OpenAI-compatible
# endpoint (see GROQ_BASE_URL in generator.py) rather than a separate `groq`
# package, so no extra dependency is needed to use Groq.
